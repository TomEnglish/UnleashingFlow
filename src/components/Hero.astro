<section class="hero">
  <div class="hero-inner">
    <p class="hero-label">Fractional AI Officer (Chief AI Officer)</p>
    <h1 class="hero-title">
      Helping small businesses unlock their bottom line — and grow faster with AI.
    </h1>
    <p class="hero-subtitle">
      Unleashing Flow works with owner-operated businesses doing $2M to $20M. We find what's slowing your operation down, fix it, and build the AI tools — web and mobile — that compound the result.
    </p>
    <div class="hero-actions">
      <a href="https://calendly.com/elliott-english/discovery-call" target="_blank" rel="noopener" class="btn btn-primary">Book a discovery call</a>
      <a href="/services" class="btn btn-outline">See how we work</a>
    </div>
  </div>
</section>

<style>
  .hero {
    background: var(--charcoal);
    color: var(--ivory);
    padding: 100px 60px 80px;
    text-align: center;
  }

  .hero-inner {
    max-width: 820px;
    margin: 0 auto;
  }

  .hero-label {
    font-family: var(--font-heading);
    font-size: 11px;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: var(--terracotta-light);
    margin-bottom: 24px;
    font-weight: 600;
  }

  .hero-title {
    font-size: clamp(32px, 5vw, 48px);
    font-weight: 800;
    line-height: 1.15;
    margin-bottom: 24px;
    color: var(--ivory);
  }

  .hero-subtitle {
    font-family: var(--font-body);
    font-size: 17px;
    line-height: 1.7;
    color: var(--taupe);
    max-width: 680px;
    margin: 0 auto 40px;
  }

  .hero-actions {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .hero .btn-outline {
    color: var(--terracotta-light);
    border-color: var(--terracotta-light);
  }

  .hero .btn-outline:hover {
    background: var(--terracotta-light);
    color: var(--charcoal);
  }

  @media (max-width: 768px) {
    .hero {
      padding: 60px 24px 48px;
    }

    .hero-actions {
      flex-direction: column;
      align-items: center;
    }
  }
</style>
