<section class="founder-note section">
  <p class="section-label">A note from Elliott</p>
  <div class="note-block">
    <p class="note-para">
      I spent twenty years running operations inside organizations much bigger than yours — the Pentagon, Morgan Stanley, healthcare, pharma, a telehealth division I built from scratch. I'm now working with small businesses for one reason: they have the same operational problems as a billion-dollar enterprise, but the leaks are visible, the fixes pay back faster, and the owner is in the room when the decision gets made.
    </p>
    <p class="note-para">
      Most of what's costing your business money isn't visible until someone looks carefully. That's the work. I find it, quantify it, and use AI as the leverage that lets one senior operator serve a $5M business at a fraction of what a full-time hire would cost.
    </p>
    <cite class="note-cite">
      <strong>Elliott English</strong>
      <span>Founder, Unleashing Flow</span>
    </cite>
  </div>
</section>

<style>
  .founder-note {
    text-align: center;
  }

  .note-block {
    max-width: 720px;
    margin: 0 auto;
    background: #fff;
    border: 1px solid #E8E0D6;
    border-radius: 16px;
    padding: 48px 44px;
    text-align: left;
  }

  .note-para {
    font-family: var(--font-body);
    font-size: 17px;
    line-height: 1.75;
    color: var(--charcoal);
    margin-bottom: 20px;
  }

  .note-para:last-of-type {
    margin-bottom: 28px;
  }

  .note-cite {
    font-style: normal;
    display: flex;
    flex-direction: column;
    gap: 4px;
    border-top: 1px solid #E8E0D6;
    padding-top: 20px;
  }

  .note-cite strong {
    font-family: var(--font-heading);
    font-size: 14px;
    font-weight: 600;
    color: var(--charcoal);
  }

  .note-cite span {
    font-family: var(--font-heading);
    font-size: 12px;
    color: var(--warm-gray);
    letter-spacing: 1px;
  }

  @media (max-width: 768px) {
    .note-block {
      padding: 32px 24px;
    }
    .note-para {
      font-size: 16px;
    }
  }
</style>
